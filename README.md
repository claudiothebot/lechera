# Milk Dreams

Prototype. Pure Three.js, no React.

## Dev

```bash
pnpm install
pnpm dev
```

Open the printed URL. Click the canvas to lock the pointer.

- WASD: move
- Mouse: look
- R: restart
- Esc: release pointer

## Status

V0 — validating the *don't spill* core loop. See `AGENTS.md`.
